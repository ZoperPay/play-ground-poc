
name: First Github Action Pipeline

run-name: Deploy by @${{ github.actor }}

on:
    push:
    workflow_dispatch:

jobs:
    print-username:
        name: name printing
        runs-on: ubuntu-latest
        steps:
        - name: Print the input name to STDOUT
          run: echo The username is Victor
              